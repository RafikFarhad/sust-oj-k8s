apiVersion: apps/v1
kind: Deployment
metadata:
  name: mastercode-queue-deploy
  namespace: sourcecode
spec:
  selector:
    matchLabels:
      app: code
      env: prod
      type: mastercode-queue
  replicas: 1
  template:
    metadata:
      labels:
        app: code
        env: prod
        type: mastercode-queue
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: cloud.google.com/gke-preemptible
                operator: Exists
      containers:
      - name: mastercode-queue-pod
        image: gcr.io/source-code-oj/mastercode-queue:0.0.6
        volumeMounts:
        - name: mastercode-config-volume
          mountPath: /var/www/html/.env
          subPath: .env
          readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 128M
          limits:
            cpu: 150m
            memory: 256M
      volumes:
      - name: mastercode-config-volume
        configMap:
          name:  mastercode-configs